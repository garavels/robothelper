# --- Core pipeline ---
cyberwave>=0.3.31          # robot control (UGV Beast) — install when connecting a robot
numpy>=1.26.0
opencv-python>=4.8.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0           # InterHuman live stream + frontend WebSocket
requests>=2.32.0
httpx>=0.27.0
openai>=1.40.0            # OpenAI vision planner (the rescue "brain")

# --- Optional: local YOLO injury pre-filter (Phase 2, set USE_YOLO=true) ---
ultralytics>=8.3.0
joblib>=1.4.0
scikit-learn>=1.5.0
